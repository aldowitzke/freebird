<div class="container text-center">
  <% @events.each do |event| %>
    <p>Meu projeto: <%= event.project.name %></p>
    <p>Data: <%= event.datetime %></p>
    <p>Quem me contratou: <%= event.user.first_name %> <%= event.user.last_name %>
  <% end %>
</div>
